const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));setTimeout(()=>document.querySelectorAll('.reveal').forEach(e=>e.classList.add('in')),1200);
addEventListener('scroll',()=>{const d=document.documentElement;document.querySelector('.progress').style.width=((d.scrollTop/(d.scrollHeight-d.clientHeight))*100)+'%'});
document.querySelector('#requestForm').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget);const m=`Hi Mobile Mechanic Cowboy, I'd like to request service.%0A%0AName: ${encodeURIComponent(f.get('name'))}%0ALocation: ${encodeURIComponent(f.get('location'))}%0AVehicle: ${encodeURIComponent(f.get('year'))} ${encodeURIComponent(f.get('vehicle'))}%0AService: ${encodeURIComponent(f.get('service'))}%0AIssue: ${encodeURIComponent(f.get('issue')||'N/A')}`;location.href=`sms:+18035493138?&body=${m}`});
